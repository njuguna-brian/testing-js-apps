// beforeEach(clearDatabase);
// describe("addItemToCart", () => {
//   const insertInventoryItem = () => {};
//   let item;
//   beforeEach(async () => {
//     item = await insertInventoryItem();
//   });
//   test("add an available item to the cart", () => {});
//   test("add unavailable item to the cart", () => {});

//   test("add multiple items to the cart", () => {});
// });

// describe("removeItemFromCart", () => {
//   test("remove item from cart", () => {});
// });

// describe("checkout", () => {
//   const mockPaymentServices = () => {};

//   beforeAll(mockPaymentServices);
//   test("checkout non-existing cart", () => {});
// });

// afterAll(destroyDbConnection);

test("always pass", () => {
  let b = 0;
  expect(b).toBe(0);
});
